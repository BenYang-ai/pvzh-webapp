# Supabase 联网(M5)。复制为 .env.local 填真实值。缺失 → 联网关闭,只剩本地 god-view。
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-PUBLIC-KEY

# 家庭访问口令(可选)。留空则用默认 "summit close"。注意:前端值,任何人读代码可见——只挡陌生人,非真安全。
VITE_ROOM_SECRET=summit close
